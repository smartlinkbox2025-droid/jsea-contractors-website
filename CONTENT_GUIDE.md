# دليل المحتوى

## إضافة خدمة

أضف عنصرًا جديدًا إلى `src/data/services.ts` مع `slug` فريد، عنوان وملخص ووصف بالعربية والإنجليزية، نقاط النطاق، القطاعات والصورة المحلية. ستظهر الخدمة تلقائيًا في صفحة القطاع وصفحتها التفصيلية.

## إضافة مشروع

أضف مشروعًا إلى `src/data/projects.ts` باستخدام `slug` فريد واسم ومدينة ونوع عمل باللغتين. استخدم أحد القطاعات الموجودة في النوع، ولا تضف قيمة أو تاريخًا أو وصفًا غير موثق.

## إضافة فئة توريد

أضف عنوان الفئة وتفصيلها في `src/data/tradingCategories.ts`. القائمة أمثلة وليست حصرًا، ويجب أن تبقى صياغة الفحص والشحن والتخليص بصيغة «تنسيق» ما لم تتوفر مستندات تثبت التنفيذ المباشر.

## الصور

ضع الصور داخل `public/assets/`، استخدم أسماء وصفية، وأضف نصًا بديلًا في المكوّن الذي يعرضها. لا تعرض صورة توضيحية على أنها صورة مشروع محدد.